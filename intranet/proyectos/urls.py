from django.conf.urls import *

urlpatterns = patterns('',
    # Examples:
    # url(r'^$', 'intranet.views.home', name='home'),
    # url(r'^blog/', include('blog.urls')),

    #url(r'^:id', 'proyectos.views.get_json_proyectos'),
)
